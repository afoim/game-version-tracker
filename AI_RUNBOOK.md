# AI_RUNBOOK

执行本仓库的游戏版本维护任务。

本轮需要处理全部 9 个游戏：

1. 原神
2. 崩坏：星穹铁道
3. 崩坏3
4. 绝区零
5. 鸣潮
6. 明日方舟：终末地
7. 异环
8. 蔚蓝档案（日服）
9. 星塔旅人（国服）

读取仓库最新状态和 `data/games.json`，使用公开 Web 来源核验全部 9 个游戏，根据核验结果更新数据，并在需要时通过 GitHub 连接器提交修改。

整个任务必须在当前这一轮从头到尾完成。不要处理到一半就结束，不要等待用户确认，不要把剩余工作留到下一轮。某个来源或某个游戏遇到问题时，继续完成其余游戏。

## 数据规范

维护 `data/games.json` 时：

- 必须保留全部 9 个游戏和现有前端字段，不能提交残缺数据。
- 维护当前版本、主要内容、下一版本、前瞻状态、当前 UP / 卡池及相关日期和天数字段。
- 只写入有可靠来源支持的信息；未确认的信息不要伪装成官方确认。
- 已结束的卡池不能继续作为当前 UP。
- 日期和天数字段应与本轮核验结果一致，天数不能为负。
- 每个游戏保留 `sources` 数组。来源包含：`title`、`url`、`type`、`claims`、`checked_at`。
- `type` 使用：`official_api`、`official`、`official_community`、`secondary`。
- `sources` 应反映本轮实际用于核验的信息来源；第三方来源不要标成官方来源。
- 不确定或无法核实的字段优先保留已有可靠数据，不要猜测覆盖。

执行过程中不要向用户输出任何进度、解释、报告或中间内容。

完成全部 9 个游戏的核验、必要的数据更新和提交判断后，最终回复必须且只能是：

已完成
