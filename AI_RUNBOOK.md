# ChatGPT Scheduled Maintenance Runbook

本仓库采用**纯 AI 驱动维护**。唯一维护执行者是 ChatGPT 定时任务，唯一仓库操作方式是 ChatGPT 自带 GitHub 连接器。

仓库**故意不包含** Python tracker、collector、adapter、tests 或 GitHub Actions。未来维护任务不得重新引入这些旧链路，除非用户明确要求改变架构。

禁止依赖 AgentDock、用户电脑、本地路径、git CLI、本地 clone、服务器常驻进程或其他外部执行环境。

## Mission

直接维护 `data/games.json`，保证 9 个游戏的数据准确、完整、可追溯，并保持现有前端字段向后兼容。

支持游戏：

- 原神
- 崩坏：星穹铁道
- 崩坏3
- 绝区零
- 鸣潮
- 明日方舟：终末地
- 异环
- 蔚蓝档案（日服）
- 星塔旅人（国服）

## 强制完成门槛

以下规则是硬门槛，不满足时**不得把本轮称为完成**：

1. 读取 GitHub 仓库只是准备阶段，绝不能因为仓库结构正常、`sources` 已存在或当前 JSON 可解析就提前结束。
2. 每一轮都必须继续进入公开互联网核验阶段，逐个处理全部 9 个游戏。
3. 对每个游戏至少要有一次**本轮实际成功的外部来源读取**。搜索结果标题、搜索摘要、旧 `sources`、模型记忆都不算本轮核验。
4. 用搜索发现来源后，必须实际打开/读取对应来源页面、API、官方社区帖子或认证账号内容，确认它确实支持相应 claim，才能刷新该来源的 `checked_at`。
5. 如果某个游戏经过合理搜索后仍无法成功读取可靠来源，应将该游戏标记为“本轮核验失败/来源不可用”，保留最后已验证数据和旧 `sources`；不能把整轮描述成“9 个游戏已核验”。
6. 如果本次任务运行环境没有公开 Web 搜索/网页读取能力，应立即把它作为**平台级阻塞**报告；不得用“数据看起来没变化”“仓库检查正常”替代联网核验，也不得刷新任何 `checked_at`。
7. 即使最终没有任何事实变化，也必须先完成 9 个游戏的外部核验，再得出“无需提交”的结论。
8. 最终报告必须逐项列出 9 个游戏，并对每个游戏给出：`成功核验 / 核验失败`、本轮实际读取的主要来源 URL、是否发现变化。少一个游戏都视为本轮未完整执行。

## 每轮执行流程

1. 使用 GitHub 连接器重新读取默认分支最新状态，至少读取 `README.md`、`AI_RUNBOOK.md`、`data/games.json`，并记录当前 HEAD。禁止使用旧缓存直接写回。
2. **不得在此步骤后结束。** 仓库读取成功后必须开始联网核验全部 9 个游戏。
3. 对每个游戏分别搜索并实际读取公开来源，至少检查：
   - 当前版本；
   - 当前版本实际开始时间；
   - 下一版本；
   - 下一版本上线时间或预计时间；
   - 前瞻/直播状态；
   - 当前版本主要内容；
   - 当前 UP / 卡池角色；
   - 卡池开始时间；
   - 卡池结束时间。
4. 信源优先级：官方 API / 官网 > 官方公告 / 官方社区 > 官方认证账号 > 可靠二级来源。不得使用社区猜测覆盖官方信息。
5. 来源类型必须按**实际来源身份**分类，而不是按内容看起来是否像官方来分类：
   - `official_api`：官方直接提供的 API；
   - `official`：游戏官网、官方新闻/公告站；
   - `official_community`：官方主体在官方社区或认证平台账号直接发布的内容；
   - `secondary`：媒体、镜像、攻略站、聚合站或其他非官方主体。
   第三方转载即使注明“来源：官方”，也不能标成 `official` 或 `official_community`。
6. 搜索结果摘要只能用于发现来源，不能直接作为修改版本、日期、卡池或 `checked_at` 的证据。必须实际读取目标页面。
7. 直接修改 `data/games.json`。不要寻找或运行 collector，因为仓库不存在 collector。
8. 每个游戏维护 `sources` 数组。每条来源必须包含：
   - `title`；
   - 非空 `url`；
   - `type`：`official_api`、`official`、`official_community`、`secondary` 之一；
   - 非空 `claims`；
   - 可解析的 ISO 8601 `checked_at`。
9. 只刷新本轮真正成功读取并用于核验的来源。某个旧来源本轮打不开、被 WAF/429 拒绝、只看到搜索摘要或根本没有重新访问时，不得刷新它的 `checked_at`。
10. 同一 URL 不重复。一个来源的 `claims` 只能写它本轮实际支持的字段，不能因为它是官网首页就把所有 claim 都挂上去。
11. 根据已核实日期重新计算天数类字段；天数不得为负。日期跨日、跨月、跨年和时区必须按来源所在地/服务器语义处理。
12. 重点防止以下错误：
   - `3.9` 与 `3.10` 按字符串错误排序；
   - 前瞻版本覆盖当前版本；
   - 直播日期被当作上线日期；
   - 卡池已结束仍显示当前角色；
   - 已进入第二期却继续显示第一期角色；
   - 未官宣版本被历史周期标成“确认”；
   - 旧版本日期被复用于新版本；
   - WAF / 429 / 临时网络错误导致错误清空数据；
   - 某一游戏核验失败后把完整 JSON 覆盖为残缺数据；
   - 将第三方转载错误标记成官方来源；
   - 未实际访问来源却刷新 `checked_at`。
13. 如果某个游戏本轮无法获得足够可靠的新证据：保留它上一次已验证的数据和原有来源，不要编造，也不要假装本轮已核验成功；在最终报告中明确说明具体失败原因。
14. 只有在全部 9 个游戏都已经尝试过外部核验之后，才进入提交判断。不能因为前几个游戏没有变化就提前结束剩余游戏。
15. 提交前做逻辑验证：
    - `games` 中恰好有 9 个目标游戏；
    - 原有前端字段全部保留；
    - 每个游戏都有合法 `sources`；
    - URL 非空；
    - ISO 日期可解析；
    - 天数不为负；
    - 已结束卡池不伪装成进行中；
    - 当前版本不会被未来前瞻覆盖；
    - 预计时间与官方确认时间语义明确区分；
    - 本轮刷新过的每个 `checked_at` 都对应一次真实成功的来源读取。
16. 写入前再次读取默认分支 HEAD 与 `data/games.json`。如果远端在本轮过程中发生变化，只整合兼容修改；不得 force push，不得覆盖无关并行改动。
17. 仅在存在实质变化时通过 GitHub 连接器提交并推送。实际重新核验并刷新有效 `checked_at` 属于有意义的数据变化；如果没有事实变化且也没有需要刷新/纠正的来源记录，则不要制造空提交。
18. 最终报告必须包含：9 个游戏逐项核验状态、本轮实际读取的主要信源 URL、发生变化的字段、未确认项/失败原因、commit hash 和 push 状态。

## 数据契约

每个游戏继续保留现有前端字段，包括但不限于：

- `game_name`
- `current_version`
- `current_content`
- `current_version_days`
- `next_version`
- `next_content`
- `preview_status`
- `days_to_next_version`
- `current_up_characters`
- `current_up_start_at`
- `current_up_end_at`
- `current_up_days_remaining`
- `sources`

`sources` 是正式数据契约的一部分，不得静默删除。

## 架构约束

以下目录/文件在纯 AI 架构中不应存在：

- `src/`
- `tests/`
- `.github/workflows/`
- Python tracker / adapter / parser

不要因为“需要验证”而自动重建它们。验证由 AI 对数据结构、日期、来源、公开网页实际读取结果和 GitHub diff 的直接检查完成。
