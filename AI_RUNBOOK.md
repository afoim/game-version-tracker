# ChatGPT Scheduled Maintenance Runbook

本仓库只由 ChatGPT 定时任务维护。仓库操作只使用 ChatGPT 自带 GitHub 连接器；数据核验只使用公开 Web 来源。

## 最重要的规则：一轮必须跑完

**除非 GitHub 或公开 Web 能力整体不可用，否则禁止提前返回。**

以下情况都不能结束整轮：

- 仓库读取成功；
- JSON 可解析；
- 前几个游戏没有变化；
- 某个来源打不开、WAF、429 或超时；
- 某个游戏暂时找不到可靠来源；
- 某个游戏核验失败。

出现单个游戏失败时：**保留旧数据 → 记录失败原因 → 继续下一个游戏**。

只有 9 个游戏全部完成至少一次外部核验尝试后，才能进入提交判断和最终报告。

## 固定执行顺序

每轮严格按下面顺序执行，不拆成下一轮，不等待用户确认。

### 1. 读取最新仓库

读取默认分支最新：

- HEAD
- `README.md`
- `AI_RUNBOOK.md`
- `data/games.json`

这只是准备步骤，**读取完成后不得结束**。

### 2. 逐个核验全部 9 个游戏

按固定顺序处理：

1. 原神
2. 崩坏：星穹铁道
3. 崩坏3
4. 绝区零
5. 鸣潮
6. 明日方舟：终末地
7. 异环
8. 蔚蓝档案（日服）
9. 星塔旅人（国服）

每个游戏都执行：

1. 搜索当前可靠来源。
2. **实际打开并读取来源**；搜索标题/摘要、旧 `sources`、模型记忆都不能作为核验完成的依据。
3. 尽量核验：当前版本、当前版本开始时间、下一版本、下一版本时间、前瞻状态、主要内容、当前 UP、UP 开始/结束时间。
4. 优先使用：官方 API / 官网 > 官方公告 / 官方社区 / 官方认证账号 > 可靠二级来源。
5. 如果首选来源失败，尝试其他可靠来源。
6. 如果仍无法可靠核验，保留该游戏旧数据和旧来源，记录“核验失败”及原因，然后继续处理剩余游戏。

**禁止因为任何一个游戏失败而停止。**

### 3. 全部 9 个完成后统一整理数据

只有完成第 2 步全部 9 个游戏后才决定是否修改 `data/games.json`。

规则：

- 只写入有可靠证据支持的变化。
- 不确定内容保持“预计 / 未确认”语义，不得把推算写成官宣。
- 前瞻/直播日期不能当作版本上线日期。
- 已结束卡池不能继续显示为当前 UP。
- 不得把完整 9 游戏 JSON 改成残缺数据。
- 保留现有前端字段，避免破坏兼容性。
- 天数类字段按已核实日期重新计算，不能为负。

`sources`：

- 只记录本轮实际读取并用于核验的来源；
- 每条包含 `title`、`url`、`type`、`claims`、`checked_at`；
- `type` 只能是 `official_api`、`official`、`official_community`、`secondary`；
- 只有本轮真正成功读取的来源才能刷新 `checked_at`；
- 同一 URL 不重复；
- 第三方转载不能标成官方来源。

### 4. 校验并写入

提交前确认：

- 9 个目标游戏全部存在；
- 原有前端字段仍保留；
- JSON 结构完整；
- 来源 URL 非空；
- `checked_at` 是有效 ISO 8601；
- 天数不为负；
- 当前版本没有被未来版本覆盖；
- 已结束卡池没有伪装成进行中。

写入前再次读取最新 HEAD 和 `data/games.json`。如果远端有并行修改，只合并兼容变化，禁止 force push、禁止覆盖无关修改。

有实质变化时用 GitHub 连接器提交；没有实质变化时不制造空提交。

### 5. 最终报告

最终报告必须一次性覆盖全部 9 个游戏，每个游戏都写明：

- `成功核验` 或 `核验失败`；
- 本轮实际读取的主要来源 URL；
- 是否发现变化；
- 如失败，写明原因。

最后补充：

- 修改了哪些字段；
- 未确认项；
- commit hash；
- push 状态。

如果少了任何一个游戏，说明本轮还没有完成，必须继续执行，不能先返回进度报告。

## 架构约束

不要寻找、创建或运行以下内容：

- Python tracker / collector / adapter / parser；
- `src/`；
- `tests/`；
- `.github/workflows/`。

不要依赖 AgentDock、用户电脑、本地路径、git CLI、本地 clone 或常驻服务器。
